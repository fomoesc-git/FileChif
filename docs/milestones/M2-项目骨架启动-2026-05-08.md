# M2 项目骨架启动（2026-05-08）

## 里程碑状态

- 状态：本阶段完成（环境限制已记录）
- 时间：2026-05-08
- 目标：建立 `filechif` 可扩展工程骨架，落地最小后端命令占位

## 本次完成

1. 创建项目主目录：`filechif/`
2. 初始化后端骨架：`src-tauri/`
3. 建立前端页面骨架：`src/pages/`
4. 创建本地数据目录：`data/`
5. 实现 `health_check` 结构化响应占位

## 下一步

- 接入真实 Tauri 配置与命令注册
- 落地 `convert_markdown_to_docx/pdf` 最小闭环
- 写入 history 记录（成功/失败）

## 第二阶段进展（2026-05-08）

- 新增转换命令：`convert_markdown_to_docx`、`convert_markdown_to_pdf`
- 新增历史查询命令：`list_history`
- 转换链路采用 `pandoc input -o output` 最小实现
- 成功/失败写入 `filechif/data/history.json`
- 当前为后端函数级闭环，下一步接入 Tauri command/invoke

## 第三阶段进展（2026-05-08）

- 已接入 Tauri command 注册：`health_check`、`convert_markdown_to_docx`、`convert_markdown_to_pdf`、`list_history`。
- 已在前端工作台接入最小调用按钮：通过 `invoke("health_check")` 触发并显示结果。
- 已完成一次端到端链路验证（前端 invoke -> Tauri command -> Rust 结构化响应）。

## 第四阶段进展（2026-05-08）

- 后端转换命令已加固：统一 `ConvertRequest`、结构化错误码、输入路径校验、输出扩展名校验、输出目录写入校验。
- 前端工作台已扩展为单任务转换界面：输入路径、输出路径、格式选择、可选模板路径、执行结果展示。
- 历史记录已闭环：转换成功/失败都会写入 `data/history.json`，前端可刷新展示历史。
- 模板参数已接入：`template_path` 透传给 pandoc `--reference-doc`。
- 验证结果：`cargo test --manifest-path src-tauri/Cargo.toml` 通过，3 个测试全绿；`cargo check --manifest-path src-tauri/Cargo.toml` 通过。
- 真实转换验证：已通过 Homebrew 安装 `pandoc 3.9.0.2`，并将测试升级为 pandoc 可用时真实生成 DOCX 文件。
- PDF 转换修复：已安装 `typst 0.14.2`，PDF 命令改为使用 pandoc `--pdf-engine=typst`，真实 PDF 生成测试通过。
- 前端构建验证：已删除本机 `.npmrc` 中与 nvm 冲突的 `prefix` 配置，保留 npm registry；`npm install` 与 `npm run build` 已通过。
