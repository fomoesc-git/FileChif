# M7 产品化基础（2026-05-09）

## 里程碑状态

- 状态：Step 1-5 已完成并通过本地验证
- 时间：2026-05-09
- 目标：把 FileChif 从可用预览版推进到更适合开源展示和团队试用的产品化基础版本。

## 范围

- 统一显示品牌为 `FileChif`。
- 保留底层数据目录 `filechif`，避免破坏 M5/M6 已迁移数据。
- 增加关于页与版本信息展示。
- 增加示例 Markdown 素材。
- 更新应用图标。
- 补齐开源仓库首页 README。

## Step 1 品牌与应用信息

- Tauri `productName` 改为 `FileChif`。
- 主窗口标题改为 `FileChif`。
- 应用导航增加品牌区。
- 后端状态接口显示应用名称 `FileChif`。

## Step 2 关于页

- 导航增加“关于”入口。
- 关于页显示产品 tagline、说明、维护者、许可状态、版本和发布通道。
- 设置页继续保留依赖、数据目录、更新说明入口。

## Step 3 示例素材

- 新增 `filechif/examples/sample-report.md`。
- 新增 `filechif/examples/README.md`。
- 示例文件覆盖标题、段落、列表、表格和转换验收步骤。

## Step 4 图标

- 更新 PNG 图标。
- 更新 macOS `icon.icns`。
- 更新 Windows `icon.ico`。

## Step 5 开源首页

- 新增仓库根目录 `README.md`。
- 说明功能、开发命令、依赖、示例文件、文档入口和数据目录兼容策略。

## 验收计划

- `npm run build`
- `cargo check --manifest-path src-tauri/Cargo.toml`
- `cargo test --manifest-path src-tauri/Cargo.toml`
- 推送 GitHub 后确认 Actions macOS/Windows 构建。

## 本地验证记录

- 验证时间：2026-05-09。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，共 6 项测试通过。
- `npm run tauri -- build --no-bundle`：通过，release binary 构建成功。

## 后续待验收

- GitHub Actions macOS/Windows 构建。
- macOS 安装包文件名从 `filechif` 迁移到 `FileChif` 后的 DMG 验收。
- Windows artifact 图标与应用名显示验收。
