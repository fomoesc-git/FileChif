# M4 分发预览与发布准备（2026-05-08）

## 里程碑状态

- 状态：待验收
- 时间：2026-05-08
- 目标：从本地 `.app` 预览版推进到可分发预览包，并补齐发布前验收资料

## M4 计划

1. 正式占位图标集
   - 生成合法 PNG 多尺寸图标与 macOS `.icns`。
   - 替换 M3 使用的 1px 占位图标。

2. DMG 分发包
   - 修复 Tauri DMG 打包链路。
   - 生成可分发 `.dmg`。

3. 安装后数据目录策略
   - 检查当前 `data/history.json`、`data/templates.json` 的发布态路径风险。
   - 明确 M4 是否迁移到系统 app data 目录。

4. 发布验收清单
   - 记录 `.app`、`.dmg` 产物路径。
   - 记录试用流程、已知限制和下一阶段建议。

## 第一阶段进展

- 已生成 1024x1024 正式占位 PNG 图标。
- 已导出 Tauri 使用的 `32x32.png`、`128x128.png`、`128x128@2x.png`。
- 已替换 M3 的 1px 占位图标。

## 第二阶段进展

- 已修复并跑通 Tauri DMG 打包链路。
- 生成 `.app` 产物：`filechif/src-tauri/target/release/bundle/macos/filechif.app`。
- 生成 `.dmg` 产物：`filechif/src-tauri/target/release/bundle/dmg/filechif_0.1.0_aarch64.dmg`。
- 备注：DMG 打包需要非沙箱环境运行，沙箱内会在 `bundle_dmg.sh` 阶段失败。

## 第三阶段评估

- 当前数据文件仍位于项目内 `data/history.json` 与 `data/templates.json`。
- M4 预览包可用于本机验收；正式分发前建议在 M5 迁移到系统 app data 目录。

## M4 验收清单

- `.app` 可生成。
- `.dmg` 可生成。
- 图标 PNG 已替换为非 1px 占位。
- `npm run build` 通过。
- `cargo test --manifest-path src-tauri/Cargo.toml` 通过。
- 打包产物路径已记录。

## 验证结果

- `npm run build` 通过。
- `cargo test --manifest-path src-tauri/Cargo.toml` 通过，5 个测试全绿。
- `npm run tauri -- build` 在非沙箱环境通过，已生成 `.app` 与 `.dmg`。
