# M1 规范基线建立（2026-05-08）

## 里程碑状态

- 状态：已完成
- 时间：2026-05-08
- 目标：建立可跨对话复用的技术规范与里程碑机制

## 本次交付

1. 建立文档中心目录：`docs/`
2. 建立规范目录：`docs/specs/`
3. 建立里程碑目录：`docs/milestones/`
4. 完成 8 份 MVP 技术规范文档
5. 建立里程碑命名与沉淀规则

## 决策结论

- MVP 冻结范围：输入（MD/TXT/AI文本）与输出（DOCX/PDF）优先。
- 架构路线：Tauri + Pandoc，AI 能力后插。
- 存储策略：MVP 先 JSON，后续可迁移 SQLite。
- 接口策略：统一 command 返回结构与错误码。

## 当前风险与待办

- 风险：模板占位符高级语法（条件/循环）尚未实现，仅定义为预留。
- 待办：进入 M2，完成项目骨架与最小转换闭环。

## 下一里程碑（M2）建议验收标准

- Tauri 工程可运行。
- `health_check` 命令可调用。
- `md -> docx/pdf` 最小链路打通。
- history 可记录成功/失败任务。

## 命名决策补充（2026-05-08）

- 产品正式名称统一为：`filechif`
- 后续所有规范、代码、界面文案统一使用 `filechif`
- 历史出现的 `FileChef`、`DocHub` 视为旧命名，不再使用
