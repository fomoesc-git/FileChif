# M8 多输入转换能力（2026-05-09）

## 里程碑状态

- 状态：Step 1 已完成并通过本地验证
- 时间：2026-05-09
- 目标：把 FileChif 从“本地 Markdown 文件转换器”扩展为支持多输入来源的文档工作台。

## M8 范围

- 支持粘贴 Markdown 文本并转换为 DOCX/PDF。
- 为后续拖拽文件、自动判定任务、模板主动选择、AI 字段识别预留交互动线。
- 保持当前 pandoc/typst 转换管线稳定。

## Step 1 粘贴 Markdown 转换

- 后端新增 `TextConvertRequest`。
- 后端新增 `convert_text_to_docx` 和 `convert_text_to_pdf` 命令。
- 粘贴内容会保存为应用数据目录下的临时 Markdown 源文件，再复用现有 pandoc 转换管线。
- 前端工作台新增输入来源切换：`本地文件` / `粘贴 Markdown`。
- 粘贴模式支持来源名称和 Markdown 文本输入。
- 历史记录会保留粘贴内容生成的临时源文件路径。

## 后续 Step 规划

- Step 2：拖拽文件进入工作台，识别 `.md`、`.markdown`、`.txt`、`.docx`。
- Step 3：根据输入来源和扩展名自动推荐输出格式。
- Step 4：DOCX 输出时主动弹出/展示模板选择区。
- Step 5：增加转换前预检面板，集中展示输入、输出、模板、依赖状态。
- Step 6：为 AI 字段识别预留模板字段确认 UI。

## 验收计划

- `npm run build`
- `cargo check --manifest-path src-tauri/Cargo.toml`
- `cargo test --manifest-path src-tauri/Cargo.toml`
- 手动粘贴 Markdown，分别转换 DOCX/PDF。

## Step 1 验证记录

- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，共 8 项测试通过。
