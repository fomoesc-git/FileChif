# M6 内部团队分发与维护自动化（2026-05-09）

## 里程碑状态

- 状态：Step 1-5 已完成并通过本地验证
- 时间：2026-05-09
- 目标：面向内部团队使用，建立稳定的构建、打包、归档和验收流程

## M6 范围

- 不做 Apple Developer 签名和公证。
- 不做公网发布。
- 聚焦工作室内部安装、更新、验收、回滚。

## Step 1 内部发布脚本与归档

- 新增一键内部发布脚本。
- 自动执行前端构建、Rust 检查、Rust 测试、Tauri 打包、DMG 验证。
- 自动复制 DMG 到 `releases/` 目录。
- 归档文件名包含版本、发布通道、日期和平台。

## Step 1 实现记录

- 新增脚本：`filechif/scripts/release_internal.sh`。
- 新增 npm 命令：`npm run release:internal`。
- 新增归档目录：`releases/`。
- 新增内部安装说明：`docs/INTERNAL_INSTALL.md`。

## Step 1 验证记录

- 验证时间：2026-05-09 00:23。
- 验证命令：`npm run release:internal`。
- 前端构建：通过，`tsc && vite build` 成功。
- Rust 检查：通过，`cargo check --manifest-path src-tauri/Cargo.toml` 成功。
- Rust 测试：通过，`cargo test --manifest-path src-tauri/Cargo.toml` 共 6 项通过。
- Tauri 打包：通过，生成 `.app` 与 `.dmg`。
- DMG 安装前检查：通过，`scripts/verify_dmg_install.sh` 确认 DMG、App、依赖和数据目录可用。
- 归档产物：`releases/filechif-0.1.0-preview-20260509-macos-aarch64.dmg`。

## 当前可验收版本

- App：`filechif/src-tauri/target/release/bundle/macos/filechif.app`。
- DMG：`filechif/src-tauri/target/release/bundle/dmg/filechif_0.1.0_aarch64.dmg`。
- 内部分发 DMG：`releases/filechif-0.1.0-preview-20260509-macos-aarch64.dmg`。

## 后续 Step 规划

- Step 2：补充应用内版本信息与更新说明入口。已完成。
- Step 3：增加内部验收清单与回归测试样例。已完成。
- Step 4：完善故障诊断文档，包括依赖缺失、权限、未签名启动阻拦。已完成。
- Step 5：建立版本归档与回滚策略，保留可用历史 DMG。已完成。

## Step 2-5 实现记录

- 应用设置页新增“版本与更新”区域，显示 GitHub 仓库、更新策略，并提供 GitHub、发布说明、安装说明入口。
- 后端 `get_app_status` 新增 release info 字段，集中暴露仓库、发布说明、安装说明和更新策略。
- 后端新增 `open_url` 命令，用系统默认浏览器打开可信 HTTP/HTTPS 链接。
- 新增 GitHub Actions workflow：`.github/workflows/build.yml`。
- GitHub Actions 支持 macOS 与 Windows 构建，Windows 使用 NSIS 安装包。
- 新增开源构建说明：`docs/GITHUB_RELEASE.md`。
- 新增内部验收清单：`docs/ACCEPTANCE_CHECKLIST.md`。
- 新增故障诊断文档：`docs/TROUBLESHOOTING.md`。
- 新增版本归档与回滚文档：`docs/ROLLBACK.md`。
- 新增仓库级 `.gitignore`，排除依赖、构建产物、本地数据和安装包。

## Step 2-5 验证记录

- 验证时间：2026-05-09。
- `npm run build`：通过。
- `cargo check --manifest-path src-tauri/Cargo.toml`：通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：通过，共 6 项测试通过。

## GitHub 开源与 Windows 构建

- 仓库地址：`https://github.com/fomoesc-git/FileChif`。
- Workflow：`.github/workflows/build.yml`。
- macOS artifact：`filechif-macos`。
- Windows artifact：`filechif-windows`。
- Windows 安装包格式：NSIS `.exe`。
- 注意：Windows 版本仍需要在 Windows 真机或虚拟机中完成首次安装与转换验收。
